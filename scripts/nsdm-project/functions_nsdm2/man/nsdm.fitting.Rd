% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nsdm.fitting.R
\name{nsdm.fitting}
\alias{nsdm.fitting}
\title{nsdm.fitting}
\usage{
nsdm.fitting(x, sets, mod_args, level, ncores = 1L, tmp_path)
}
\arguments{
\item{x}{An \code{nsdm.pseudoabsences} object used as the master table for feature values and SIDs}

\item{sets}{A named list of replicates produced upstream, each replicate contains \code{reg_train}, \code{glo_train}}

\item{mod_args}{A list of class \code{multi.input}, one element per model family, each with slots \verb{@mod} a function name, \verb{@args} a list of arguments, \verb{@weight} logical, \verb{@tag} a short label}

\item{level}{Character string, \code{"glo"} or \code{"reg"}, selects which subset of \code{sets} to use for SIDs}

\item{ncores}{Integer, number of cores for \code{mclapply}}

\item{tmp_path}{Character, directory for temporary files such as LightGBM saves}
}
\value{
An \code{nsdm.fit} object with fitted models stored in \verb{@fits}, grouped by \verb{@tag}, each tag holds a list of per replicate fits
}
\description{
Model fitting
}
\author{
Antoine Adde (antoine.adde@eawag.ch)
}
